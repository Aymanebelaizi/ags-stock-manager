{% extends 'base.html.twig' %}

{% block title %}Nouvelle Demande | AGS.Pro{% endblock %}

{% block body %}
<div class="container-fluid">
    {# 1. Bouton Retour #}
    <div class="mb-3">
        <a href="{{ path('admin_purchase_index') }}" class="text-muted text-decoration-none small">
            <i class="fas fa-arrow-left me-1"></i> Back to List
        </a>
    </div>

    {# 2. Titre et Sous-titre #}
    <div class="mb-4">
        <h2 class="fw-bold text-dark mb-1">Create New Purchase Request</h2>
        <p class="text-muted small">Fill in the details to submit a new material request to the AGS system.</p>
    </div>

    <div class="row g-4">
        {# 3. Formulaire (Style Produit) #}
        <div class="col-lg-8">
            <div class="card border-0 shadow-sm rounded-4 p-4 bg-white">
                <form action="{{ path('admin_purchase_new') }}" method="POST">
                    <div class="row g-4">
                        <div class="col-md-8">
                            <label class="form-label fw-bold small text-muted text-uppercase">Product Name</label>
                            <select name="product_id" class="form-select border-0 bg-light rounded-3 py-3" required>
                                <option value="" disabled selected>-- Select an item --</option>
                                {% for p in products %}
                                    <option value="{{ p.id }}">{{ p.name }} (Current Stock: {{ p.quantity }})</option>
                                {% endfor %}
                            </select>
                        </div>
                        <div class="col-md-4">
                            <label class="form-label fw-bold small text-muted text-uppercase">Quantity</label>
                            <input type="number" name="quantity" class="form-control border-0 bg-light rounded-3 py-3" min="1" placeholder="Ex: 10" required>
                        </div>
                        <div class="col-12">
                            <label class="form-label fw-bold small text-muted text-uppercase">Justification</label>
                            <textarea name="justification" class="form-control border-0 bg-light rounded-3 py-3" rows="4" placeholder="Explain why this material is needed..." required></textarea>
                        </div>
                        <div class="col-12 text-end">
                            <button type="submit" class="btn btn-primary px-5 py-2 fw-bold rounded-3 shadow-sm">
                                Submit Request
                            </button>
                        </div>
                    </div>
                </form>
            </div>
        </div>

        {# 4. Bloc Quick Tip (Identique à l'image_f492cb.png) #}
        <div class="col-lg-4">
            <div class="card border-0 shadow-sm rounded-4 p-4" style="background-color: #f0f7ff;">
                <h6 class="fw-bold text-primary mb-3">
                    <i class="fas fa-info-circle me-2"></i> Quick Tip
                </h6>
                <p class="small text-muted mb-0">
                    The request will be set to <strong>"Pending"</strong> status. Once an admin approves it, the stock will be automatically updated in the inventory.
                </p>
            </div>
        </div>
    </div>
</div>
{% endblock %}