{% extends 'base.html.twig' %}

{% block body %}
<div class="container-fluid">
    <div class="mb-3">
        <a href="{{ path('admin_supplier_index') }}" class="text-muted text-decoration-none small">
            <i class="fas fa-arrow-left me-1"></i> Back to Suppliers
        </a>
    </div>

    <div class="mb-4">
        <h2 class="fw-bold text-dark mb-1">Register New Supplier</h2>
        <p class="text-muted small">Add a new commercial partner to your procurement network.</p>
    </div>

    <div class="row g-4">
        <div class="col-lg-8">
            <div class="card border-0 shadow-sm rounded-4 p-5 bg-white">
                <form method="POST">
                    <div class="row g-4">
                        <div class="col-12">
                            <label class="form-label fw-bold small text-muted text-uppercase">Company Name</label>
                            <input type="text" name="name" class="form-control border-0 bg-light rounded-3 py-3" placeholder="Ex: Dell Morocco" required>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label fw-bold small text-muted text-uppercase">Email Address</label>
                            <input type="email" name="email" class="form-control border-0 bg-light rounded-3 py-3" placeholder="contact@company.com" required>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label fw-bold small text-muted text-uppercase">Phone Number</label>
                            <input type="text" name="phone" class="form-control border-0 bg-light rounded-3 py-3" placeholder="+212 600-000000">
                        </div>
                        <div class="col-12">
                            <label class="form-label fw-bold small text-muted text-uppercase">Full Address</label>
                            <textarea name="address" class="form-control border-0 bg-light rounded-3 py-3" rows="3"></textarea>
                        </div>
                        <div class="col-12 text-end pt-3">
                            <button type="submit" class="btn btn-primary px-5 py-2 fw-bold rounded-3">Save Partner</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>

        <div class="col-lg-4">
            <div class="card border-0 shadow-sm rounded-4 p-4" style="background-color: #f0f7ff;">
                <h6 class="fw-bold text-primary mb-3"><i class="fas fa-lightbulb me-2"></i> Quick Tip</h6>
                <p class="small text-muted mb-0">
                    Linking suppliers to your products allows for better tracking of purchase prices and restock delays in your <strong>Decision Dashboard</strong>.
                </p>
            </div>
        </div>
    </div>
</div>
{% endblock %}