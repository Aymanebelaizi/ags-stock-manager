{% extends 'base.html.twig' %}

{% block title %}Modifier Catégorie{% endblock %}

{% block body %}
<div class="container-fluid px-0">
    <div class="mb-3">
        <a href="{{ path('admin_category_index') }}" class="text-decoration-none text-muted small fw-bold text-uppercase">
            <i class="fas fa-arrow-left me-1"></i> Back to list
        </a>
    </div>

    <div class="mb-4">
        <h2 class="fw-bold text-dark">Update <span class="text-primary">Category</span></h2>
        <p class="text-muted small">Modifying category: #{{ category.id }} - {{ category.name }}</p>
    </div>

    <div class="row">
        <div class="col-lg-8">
            <div class="card border border-secondary-subtle shadow-sm rounded-4 bg-white p-4">
                <form method="POST">
                    <div class="row g-4">
                        <div class="col-12">
                            <label class="form-label fw-bold small text-muted text-uppercase">Name</label>
                            <input type="text" name="name" class="form-control form-control-lg rounded-3 border-secondary-subtle shadow-none bg-light bg-opacity-10" 
                                   value="{{ category.name }}" required>
                        </div>

                        <div class="col-12">
                            <label class="form-label fw-bold small text-muted text-uppercase">Description</label>
                            <textarea name="description" class="form-control rounded-3 border-secondary-subtle shadow-none bg-light bg-opacity-10" 
                                      rows="5">{{ category.description }}</textarea>
                        </div>
                    </div>

                    <div class="d-flex justify-content-end gap-2 mt-5 pt-3 border-top">
                        <a href="{{ path('admin_category_index') }}" class="btn btn-light border px-4 rounded-3 fw-bold">
                            <i class="fas fa-times me-1"></i> Cancel
                        </a>
                        <button type="submit" class="btn btn-primary px-4 rounded-3 fw-bold shadow-sm">
                            <i class="fas fa-sync me-1"></i> Update Category
                        </button>
                    </div>
                </form>
            </div>
        </div>
        
        <div class="col-lg-4">
            <div class="card border-0 shadow-sm rounded-4 bg-primary text-white p-4 h-100" style="background: linear-gradient(135deg, #1a73e8 0%, #1557b0 100%) !important;">
                <div class="d-flex align-items-center mb-3">
                    <i class="fas fa-info-circle me-2 fs-4"></i>
                    <h5 class="mb-0 fw-bold">Editing Info</h5>
                </div>
                <p class="small opacity-90 mb-0">
                    Updating the category name will automatically update the display for all <strong>{{ category.products|length }} products</strong> linked to it.
                </p>
            </div>
        </div>
    </div>
</div>
{% endblock %}