{% extends 'base.html.twig' %}

{% block title %}Tableau de Bord{% endblock %}

{% block body %}
<div class="row mb-4">
    <div class="col-12">
        <h2 class="text-gray-800">👋 Bonjour, {{ app.user ? app.user.email : 'Invité' }}</h2>
        <p class="text-muted">Voici l'état actuel de votre stock.</p>
    </div>
</div>

<div class="row g-4">
    <div class="col-md-4">
        <div class="card h-100 border-left-primary shadow-sm hover-card">
            <div class="card-body">
                <div class="d-flex align-items-center justify-content-between">
                    <div>
                        <h6 class="text-primary fw-bold text-uppercase mb-1">Inventaire</h6>
                        <p class="mb-0 small text-muted">Consulter la liste des produits</p>
                    </div>
                    <i class="fas fa-boxes fa-2x text-gray-300"></i>
                </div>
                <a href="{{ path('app_product_index') }}" class="stretched-link"></a>
            </div>
        </div>
    </div>

    <div class="col-md-4">
        <div class="card h-100 border-left-success shadow-sm">
            <div class="card-body">
                <div class="d-flex align-items-center justify-content-between">
                    <div>
                        <h6 class="text-success fw-bold text-uppercase mb-1">Entrée / Sortie</h6>
                        <p class="mb-0 small text-muted">Enregistrer un mouvement</p>
                    </div>
                    <i class="fas fa-exchange-alt fa-2x text-gray-300"></i>
                </div>
                <a href="{{ path('app_stock_movement_new') }}" class="stretched-link"></a>
            </div>
        </div>
    </div>

    {% if is_granted('ROLE_MANAGER') %}
    <div class="col-md-4">
        <div class="card h-100 border-left-warning shadow-sm">
            <div class="card-body">
                <div class="d-flex align-items-center justify-content-between">
                    <div>
                        <h6 class="text-warning fw-bold text-uppercase mb-1">Achat Fournisseur</h6>
                        <p class="mb-0 small text-muted">Créer une demande d'achat</p>
                    </div>
                    <i class="fas fa-shopping-cart fa-2x text-gray-300"></i>
                </div>
                <a href="{{ path('app_purchase_request_new') }}" class="stretched-link"></a>
            </div>
        </div>
    </div>
    {% endif %}
</div>
{% endblock %}