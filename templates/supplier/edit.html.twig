{% extends 'base.html.twig' %}

{% block title %}Modifier Fournisseur{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>Modifier le fournisseur</h1>
    <div class="card shadow-sm mt-3">
        <div class="card-body">
            {{ form_start(form) }}
                <div class="mb-3">{{ form_row(form.name) }}</div>
                <div class="mb-3">{{ form_row(form.email) }}</div>
                <div class="mb-3">{{ form_row(form.phone) }}</div>
                <button class="btn btn-warning">{{ button_label|default('Mettre à jour') }}</button>
                <a href="{{ path('app_supplier_index') }}" class="btn btn-secondary">Annuler</a>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}